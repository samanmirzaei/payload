import { CoreModule } from './module'

/**
 * Core modules are always installed for any project built from this starter.
 * Optional modules (commerce/blog/corporate) can be registered in a future step.
 */
export const coreModules = [CoreModule] as const
