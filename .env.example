# Copy to `.env` for local Docker Compose usage.
# Do not commit real secrets.

# Runtime
NODE_ENV=development
PORT=3000

# Payload
# Generate a long random value for production.
PAYLOAD_SECRET=change-me
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# Public read mode (for public delivery endpoints and/or collection publicRead access)
# - published: only `status='published'` is publicly readable (Pages/Posts/Products)
# - all: public can read everything (useful in dev)
# Default: all in development, published in production
# PUBLIC_READ_MODE=published|all

# Project preset (used by seeds + init script)
# PROJECT_PRESET=corporate|shop

# Optional: enable secure public Orders creation via `/api/public/orders`
# ORDER_SECRET=your-long-random-secret

# Database (Postgres)
# Compose will provide a `postgres` service; use this URL inside containers.
DATABASE_URL=postgres://payload:payload@postgres:5432/payload

# Dev seed (dev only)
PAYLOAD_RUN_SEED=false
PAYLOAD_SEED_COMMERCE=true

# Optional: Postgres container config (compose usage)
POSTGRES_USER=payload
POSTGRES_PASSWORD=payload
POSTGRES_DB=payload
